# spring-redis

-. Spring-Data-Redis 라이브러리를 사용하여, Redis 서버에 Key(String)-Value(String) 또는 Key(String)-Value(Object)의 형태로 데이터를 조회, 저장합니다.

-. Spring-Data-Redis 라이브러리는 pom.xml에 정의하여, maven을 통해 다운로드 받습니다.

-. Redis 연결에 필요한 ConnectionFactory와 Key,Value Serializer Bean은 root-context.xml에서 정의합니다.